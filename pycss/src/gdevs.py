# Gaussian gerivatives using Hermite polynomials

import numpy as np


def hermite(deg):
    """Hermite polynomial of degree deg """
    return []


def gaussian_dev(order, sigma):
    """Gaussian derivative of a given order """
    base_gaussian = np.exp(- t**2 / (2*sigma**2))

    return k

